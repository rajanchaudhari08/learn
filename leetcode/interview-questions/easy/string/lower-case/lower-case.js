/**
 * Transforms every uppercase character in the input string to lowercase.
 * @param {string} inputString - The string to convert.
 * @returns {string} - The lowercase version of the input.
 */

const toLowerCase = (inputString) => {
  return inputString.toLowerCase();
};

console.log(toLowerCase("Javascript"));

/**
 * Results: javascript
 */
